[project]
name = "unified-forecasting"
version = "0.1.0"
description = "Unified forecasting project experiments and preprocessing utilities."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel",
    "matplotlib>=3.7",
    "numpy>=1.24",
    "pandas>=2.0",
    "pyarrow",
    "scikit-learn",
    "termcolor>=3.3.0",
    "torch>=2.1",
    "tqdm>=4.65",
]

[tool.uv]
package = false
